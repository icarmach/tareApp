<h1>Listing archives</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Version</th>
    <th>Homework user</th>
    <th>Ip</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @archives.each do |archive| %>
  <tr>
    <td><%= archive.name %></td>
    <td><%= archive.version %></td>
    <td><%= archive.homework_user_id %></td>
    <td><%= archive.ip %></td>
    <td><%= link_to 'Detalles', archive %></td>
    <td><%= link_to 'Editar', edit_archive_path(archive) %></td>
    <td><%= link_to 'Destruir', archive, method: :delete, data: { confirm: '¿Seguro que desea eliminarlo?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nuevo Archivo', new_archive_path %>
